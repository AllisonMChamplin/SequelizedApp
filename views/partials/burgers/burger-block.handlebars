<li>
	<div class="burgerclass">{{burger_name}}</div>	

	<button class="change-eat btn" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}Eat it!{{else}}Put it back!{{/if}}
	</button>
	<button class="delete-burger btn" data-id="{{this.id}}">Delete it!</button>
	
</li>
